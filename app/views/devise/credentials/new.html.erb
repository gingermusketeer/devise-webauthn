<%= form_with(model: @credential, local: true, id: "js-new-credential") do |f| %>
  <%= f.text_field :nickname, name: :credential_nickname %>
  <%= f.submit %>
<% end %>

<%= javascript_tag do%>
  window.createOptions = <%= @create_options.to_json.html_safe %>;
<% end %>
